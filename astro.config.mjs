// @ts-check
import { defineConfig } from 'astro/config';

// A Severed Story — static handbook site.
// Default static output; no integrations needed (vanilla CSS + a single
// progressive-enhancement script). Add `site`/`base` here if/when this is
// deployed under a subpath (e.g. GitHub Pages).
export default defineConfig({});
